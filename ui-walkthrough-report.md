## UI 走查报告

### GameLobby
- 静态样式迁移: 未完成
- 遗留内联样式数量: 12 (均为静态)
  - 第44-54行: 网格叠加样式
  - 第64行: letterSpacing
  - 第81行: WebkitOverflowScrolling 和 maxWidth
  - 第122-125行: 标题文本阴影
  - 第131-135行: 副标题过渡
  - 第145行: 网格模板列
  - 第177-179行: "即将推出"覆盖层
  - 第232-238行: 平台入口动画
  - 第272-278行: 样式中心动画
  - 第314-328行: LAN IP横幅
  - 第359-362行: 快速加入动画
  - 第386-389行: 加入按钮样式

### Header
- 静态样式迁移: 部分完成
- 遗留内联样式数量: 2 (均为基于状态的动态样式)
  - 第55-65行: 主题按钮样式 (基于当前主题动态变化)
  - 第77-79行: 动画切换按钮颜色 (基于动画状态)

### ModeSelect
- 静态样式迁移: 未完成
- 遗留内联样式数量: 10 (均为静态)
  - 第19-23行: 微调器点样式
  - 第92行: 网格模板列
  - 第294行: 错误颜色
  - 第345行: 错误颜色
  - 第355-358行: 状态点脉冲动画
  - 第456-460行: 警告样式
  - 第512行: 错误颜色
  - 第599行: WebkitOverflowScrolling 和 maxWidth
  - 第633行: 文本阴影
  - 第737行: WebkitOverflowScrolling

### 总结
失败

### 详细分析

1. **CSS变量使用正确**: 所有组件都正确使用了CSS变量（如 `--accent-primary`、`--text-muted`、`--bg-surface` 等），支持主题切换。

2. **动态样式保留合理**: 以下动态内联样式应保留：
   - 基于 `isInstalled` 状态的样式（GameLobby）
   - 鼠标悬停效果（通过JavaScript动态修改）
   - 基于当前主题的状态样式
   - 基于 `ipCopied` 状态的按钮样式
   - 基于模式颜色的动态边框和阴影

3. **静态样式迁移不足**: 大量静态内联样式未迁移，包括：
   - 固定布局值（gridTemplateColumns、maxWidth）
   - 静态动画定义（@keyframes 应移至CSS文件）
   - 固定颜色值（如错误/警告颜色应使用CSS变量）
   - 静态过渡和变换效果

### 建议
1. 将静态内联样式迁移至CSS类
2. 将错误/警告颜色统一为CSS变量
3. 将动画关键帧移至CSS文件
4. 保留合理的动态内联样式（基于状态、主题、悬停）
5. 考虑使用CSS-in-JS或CSS模块化方案